# File 7 — `07.jpg` (Module 2 graded quiz)

**When:** After `04.pdf` + script `14`  
**Points:** 5 questions × 1 point

---

## Question 1 — File upload and count

**Jessica wants upload + count files. Which component?**

- gr.Number
- gr.Textbox
- **gr.File** ✓
- gr.Interface

**Why:** `gr.File` handles upload; your `fn` counts paths. `gr.Interface` wraps the whole app, not the upload widget.

---

## Question 2 — Text + numeric inputs together

**David wants text and numeric inputs. Which approach?**

- Use gr.Interface to define both without a list
- Custom combined component
- Separate Interface instances
- **Use a list: gr.Textbox and gr.Number** ✓

**Why:** One `fn(a, b)` → `inputs=[gr.Textbox(), gr.Number()]` — order matches parameters (`01.md`, `02.pdf`).

---

## Question 3 — Role of gr.Interface

- Used to launch the server
- Defines application logic
- **Specifies inputs and outputs for Python functions** ✓
- Provides public URL

**Why:** `launch()` starts server; `fn` holds logic; public URL comes from `launch(share=True)` or local URL from `launch()`.

---

## Question 4 — Slider outputs integer

- min and max integers only
- **min, max, value, and step all integers** ✓
- min, max, value integers
- value integer only

**Why:** `04.pdf` page 10 + `09.pdf`: step must also be integer (e.g. `step=1`).

---

## Question 5 — One of three mutually exclusive venues

- SortedList
- Checkbox
- **Dropdown** ✓
- Textbox

**Why:** Exactly **one** of three fixed options → `Dropdown` (default single select) or `Radio`. Checkbox = boolean. Textbox = free text. SortedList not in course materials.

**Note:** `Radio` would also work; Coursera answer key uses **Dropdown**.

---

## Submit checklist

- [ ] All five match cards in `01`–`04` and `09.pdf`
- [ ] Honor code checkbox ticked
- [ ] Save draft if not ready

**Next file:** [08.md.md](./08.md.md)