# File 7 — `07.md` (lesson summary)

Each bullet expanded and tied to source files.

---

## Bullet 1 — What LlamaIndex is

> Flexible framework for LLM apps; **context augmentation** via structured ingestion, chunking, indexing, retrieval.

→ `01.md` (Document, load), `02.md` (index, query)

---

## Bullet 2 — vs LangChain

> Built-in document loaders + query engines; LangChain emphasizes **chaining steps** in workflows.

→ You did M1 with LangChain `RetrievalQA` + separate Chroma ingest. M3 collapses steps with `VectorStoreIndex` / `as_query_engine()`.

---

## Bullet 3 — Conversational RAG app pattern

> Ingest LinkedIn data → chunk to **nodes** → embed/store → **response synthesizer** or **query engine** → personalized replies.

→ `05.pdf` icebreaker pipeline (mock LinkedIn JSON)

---

## Bullet 4 — Classes to know

> **document, node, index, query engine**

| Class / object | From |
|----------------|------|
| `Document` | `01.md` |
| `Node` | `01.md` after `SentenceSplitter` |
| `VectorStoreIndex` | `02.md` |
| Query engine | `02.md`, `05.pdf` Part 6 |

**Next:** [08.pdf.md](./08.pdf.md)