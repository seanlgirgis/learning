# RAG playground

For CRS 002+ — document loaders, chunking, embeddings, vector stores, retrievers.

## When you start here

1. Finish CRS 001 LangChain basics (LCEL, parsers, history).
2. Work through Coursera notebook sections on loaders / Chroma (in CRS 001 notebook 2, exercises 3–4 — preview).
3. Add your own `try_*.py` files here — copy patterns from `courses/crs_002_...` when that course migrates.

## Starter ideas (future)

- Load one PDF → print chunk count
- Same text, two splitters, compare chunk sizes
- Embed one sentence → search with one query (smallest possible RAG loop)

## Reference

- `courses/crs_002_build_rag_applications_get_started/` (scaffold)
- Coursera notebook: `Build Smarter AI Apps...` exercises 3–4 in `source_material/`