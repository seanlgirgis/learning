# Model Maps Archive

Frozen copies of the canonical TerraForm StudyBubble maps. All new builds use this style automatically via the shared `viewer/` + `build_topic.py` pipeline.

See **`MODEL_MAPS.md`** at the engine root for human recall.