# Open Questions

Decisions for Sean to confirm before we build folders.

## Structure

1. **Migrate vs link** — New courses only in `learning/`, or start migrating from StudyBook now?
2. **`shared/` folder** — Keep it, or always push reuse straight to `rag_foundation`?
3. **Catalog format** — YAML okay, or prefer Markdown-only indexes at first?

## Paths

4. **Path kinds** — Fixed list? e.g. `skill_track`, `career_track`, `professional_certificate`, `custom`
5. **Cross-provider paths** — Allow a custom path mixing Coursera + DataCamp courses? (Probably yes.)

## First concrete slice

6. **First path to model** — Coursera IBM RAG cert, DataCamp Developing AI Apps, or something else?
7. **First course to land** — Which single course gets `courses/` + `lab_01` first?

---

Add answers here when decided; then update `repository_plan.md` and `GROK_CURRENT_STATE.md`.